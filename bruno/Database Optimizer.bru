meta {
  name: Database Optimizer
  type: http
  seq: 5
}

post {
  url: http://127.0.0.1:8080/optimizer/database
  body: json
  auth: inherit
}

body:json {
  {
    "message": "Why my table database_optimizer_history uses varchar primary key, and what can I gain if I will use uuid?"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
